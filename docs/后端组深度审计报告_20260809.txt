【依神网站汇总 · 后端组深度审计与优化报告】
📅 日期：2026-08-09 09:10（v2 最终版）
提交人：数据库与后端组

━━━━━━━━━━━━━━━━━━━━━━
一、流程（严格按规范）
━━━━━━━━━━━━━━━━━━━━━━
git 跟进（main fdb7dcc v2.0 + 36 新提交：看板郎AI/拖拽部署/反馈状态/榜单多样化）
→ rebase 合并（零冲突）+ 补 jszip 依赖
→ 新迁移执行（feedback_status/work_deploys 桶/视图重建 35 列）
→ 第一轮子代理深度测试（3 并行）→ 修复
→ 第二轮子代理深度测试（2 并行）→ 修复
→ 主控复现验证 + 真实客户端验证 → 提交分支（未合并 main）

━━━━━━━━━━━━━━━━━━━━━━
二、发现并修复的问题（两轮审计 + 两轮验证）
━━━━━━━━━━━━━━━━━━━━━━
🔴 P0 安全（3 项）
1. merge_ideas 越权：管理员身份由调用方参数决定，任何人可冒充
   → is_admin() 校验（auth.uid 判定）✅ 实测冒充 400 拒
2. profiles 隐私泄露：email/phone/is_admin 任意访客可读
   → 表级 REVOKE + 列级 GRANT 白名单 ✅ 实测 403
3. yili_corpus 语料库无 RLS：依力AI知识库任意人可读写/投毒/清库
   → 启用 RLS（SELECT 公开、写仅 is_admin）+ upsert 函数校验 ✅

🔴 HIGH 功能（7 项）
4. works_with_likes 视图缺 meta 列 → 重建 35 列（AI 警告/编辑清空/播放器修复）
5. 评论回复传参错误（回复变顶级评论）→ 修复评论树
6. EditWebsitePage stale closure（管理员编辑被误拦）→ 修复
7. 搜索 or() 括号回归（postgrest-js 自动包装，双括号必挂）→ 改回无括号
8. 搜索值逗号/点号破坏 or 解析 → 双引号包裹 + 转义（实测 200）
9. comments 缺 UPDATE 策略（反馈闭环失效）→ 补作者/管理员策略
10. rpc_delete_group 0 行静默 204 → 显式校验（越权/不存在 400）

🟡 MED（6 项）
11. partitions 仅管理员（RLS+前端）；12. 匿名按钮 4 处登录引导
13. idea_comments 删除走 RPC；14. console.log 清理/分页钳制/23505 中文
15. 补索引 4 个 + source_idea_id 外键；16. jszip 依赖补全（build 修复）

━━━━━━━━━━━━━━━━━━━━━━
三、验证结果（不糊弄）
━━━━━━━━━━━━━━━━━━━━━━
✅ 第一轮子代理：功能 42/43（发现 rpc_delete_group 误报——已查证非漏洞，优化语义）
✅ 第二轮子代理：10/11（逗号搜索未达标 → 双引号方案实测修复）+ 代码回归 7/7
✅ 主控复现：rpc_delete_group 越权复现=假阳性（组未删，查证工具误判）
✅ 真实 supabase-js 客户端：中文/逗号/点号搜索全部 200
✅ npm run build：150 modules 多次通过
✅ 测试数据全清理（作品/评论/想法/分组/分区/桶 0 残留）

━━━━━━━━━━━━━━━━━━━━━━
四、提交状态
━━━━━━━━━━━━━━━━━━━━━━
✅ 分支：fix/backend-security-audit（5 commit，已推送）
✅ PR：#76 已更新（未合并 main，按流程等 PM 审核）
   https://github.com/EmenWebsiteCollection/yishen-navigation/pull/76
✅ 与最新 main fdb7dcc rebase 兼容（提交前已复查无新提交）

━━━━━━━━━━━━━━━━━━━━━━
五、遗留（LOW）
━━━━━━━━━━━━━━━━━━━━━━
- work_deploys 孤儿文件（删作品后部署文件不可删，建议后续级联清理）
- 联系邮箱占位、normalizeUrl 增强、评论分页
- 冗余对象清理（双触发器/重复策略，低优先）
